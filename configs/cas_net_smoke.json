{
    "method_name": "cas_net_smoke",
    "description": "CAS-Net, 5 epochs. A timing and smoke-test run: identical to cas_net.json except for method_name and epochs, so seconds-per-iteration and peak VRAM measured here carry over to the real run. The distinct method_name keeps its run dir and checkpoints separate from cas_net's, so a smoke checkpoint can never be mistaken for a trained model.",
    "input_type": "random_crop",
    "output_type": "mask_2ch_sigmoid",
    "data": {
        "params": {
            "patch_size": [
                128,
                160,
                160
            ],
            "min_fg_fraction": 0.5
        }
    },
    "model": {
        "name": "cas_net",
        "params": {
            "classes": 2,
            "channels": 1
        },
        "checkpoint": null
    },
    "loss": {
        "name": "multiclass_dice",
        "params": {}
    },
    "training": {
        "batch_size": 5,
        "lr": 0.0001,
        "epochs": 5
    },
    "postprocessing": {
        "steps": [
            "argmax_binarize",
            "keep_components_larger_than_100_voxels"
        ],
        "params": {}
    }
}
